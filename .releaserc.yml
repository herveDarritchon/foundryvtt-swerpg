repositoryUrl: https://github.com/herveDarritchon/foundryvtt-swerpg.git

branches:
  - main
  - next
  - '/.*cicd.*/'

plugins:
  - - '@semantic-release/commit-analyzer'
    - releaseRules:
        - type: feat
          release: minor
        - type: fix
          release: patch
        - type: chore
          release: patch
        - type: docs
          release: patch
        - type: refactor
          release: patch
        - type: style
          release: patch
        - type: perf
          release: patch
        - type: breaking
          release: minor
      parserOpts:
        noteKeywords: [ 'BREAKING CHANGE' ]
  - '@semantic-release/release-notes-generator'
  - - '@semantic-release/changelog'
    - changelogFile: CHANGELOG.md
  - './update-system-json.cjs'  # 👈 Ton script custom ici
  - - '@semantic-release/git'
    - assets:
        - CHANGELOG.md
        - system.json      # 👈 Attention, si le fichier est à la racine, ne mets pas src/
      message: |
        chore(release): ${nextRelease.version} [skip ci]

        ${nextRelease.notes}
  - - '@semantic-release/github'
    - successComment: false
      failComment: false
      assets: [ ]
